{
  "name": "swimfare",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "seed:gen": "node scripts/seed.mjs",
    "seed:local": "npm run seed:gen && wrangler d1 execute swimfare-db --local --file=scripts/seed.sql",
    "seed:remote": "npm run seed:gen && wrangler d1 execute swimfare-db --remote --file=scripts/seed.sql"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251001.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
